# smart-contact-manager-project
Creating contact manager spring boot project which is connected to database and also having role based access to the web application and different feature like insert, update and delete
Also Different user can access his contact using its own credential if user is authenticated
